const {sendMessage, editMessage} = require('./handler.js');

// sendMessage({
//     roomId: 1,
//     messageText: "ohai"
// });

editMessage({
    messageId: 50121364,
    messageText: "b 😁 d"
});
